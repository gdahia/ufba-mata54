O método 'lookup', na linha 276, lê o registro da posição 'hash(key)'. Caso o registro dessa posição seja inválido ou ilegítimo, sabemos, pela correção do método 'insert', que não há registros de 'hash(key)' e, especialmente, que 'key' não aparece no arquivo. Logo, a chave não foi encontrada. 
Caso tenhámos um registro legítimo, sabemos, pela correção do método 'insert', que todos os registros de 'hash(key)' estão encadeados. O 'while' da linha 287 percorre a lista até que encontremos o registro com 'key' ou encontremos o último registro da cadeia de 'hash(key)'. Em seguida, basta checar se essa busca foi bem-sucedida e a prova de 'lookup' está concluída.
